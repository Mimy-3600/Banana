#pragma once

//external include
#include <string>
#include <vector>

//internal include
namespace BananaExecutablePilots {
	namespace CommandFunction {
		void __version(std::vector<std::string> args);
	}
}